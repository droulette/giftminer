<%= form_for(@category_product_link) do |f| %>
  <% if @category_product_link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@category_product_link.errors.count, "error") %> prohibited this category_product_link from being saved:</h2>

      <ul>
      <% @category_product_link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :productcat %><br />
    <%= f.text_field :productcat %>
  </div>
  <div class="field">
    <%= f.label :product %><br />
    <%= f.text_field :product %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
